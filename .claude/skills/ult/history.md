# /ult — History Log

## 2026-08-18 19:0x〜（残タスク全件対応、ユーザーから「残りのタスク全部解決させよう」の依頼）

その日の初回監査(15:3x)からの4件の残課題すべてに決着をつけた。

- **Firestore/Storageルールのデプロイ**: ユーザーが実施。ただしstorage.rulesの
  chatMedia/eventsについて、firestore.exists()によるグループメンバーシップの
  cross-service検証を試みたところ「Unexpected 'firestore'」でコンソール保存自体が
  失敗した。このプロジェクトではStorage↔Firestoreのcross-service連携が未設定と
  見られるが、有効化の具体的手順を一次情報から確定できなかったため、深追いせず
  isSignedIn()のみの安全に確実デプロイできる形に撤回・再デプロイした（★残存リスク：
  グループ非メンバーでも書き込み自体は可能。ただしfirestore.rules側でメッセージ・
  イベント"ドキュメント"の作成は既にグループメンバー限定のため、被害は未使用
  ファイルによるStorage容量消費程度に留まる。cross-service連携の有効化手順が
  判明すれば再挑戦の価値あり）
- **App Check**: ユーザーの実機でEnforce解除。私自身のシミュレーターテストが
  「無効なリクエスト」の指標にノイズとして混ざっていた可能性を指摘し、実機のみの
  状態で指標を再確認するようユーザーに依頼(このセッションでの追加のシミュレーター
  テストは控えた)。Bundle ID・署名チーム・エンタイトルメントに設定ミスは無いことは
  コードから確認済み
- **Gemini API利用規約の年齢要件**: 一次情報(ai.google.dev/gemini-api/terms)で
  「18歳未満に利用される可能性が高いアプリでのAPI使用を禁止」との条項を確定。
  UserSettings.isAdultを新設(既存の誕生日データから算出、新規収集不要)し、
  現在到達可能な全Gemini呼び出し箇所(当日ガイド生成・グループ情報自動検索/修正)に
  18歳ゲートを実装。AI予定検索は現在「開発中」で到達不能だが念のため適用。
  URLからのイベント抽出機能は対象外のまま(着手しない方針のため)
- **DMへのNGワードフィルター**: 匿名/公開トークルーム・会場口コミと同じ検知・
  伏せ字化・モデレーション記録の仕組みをDirectMessageViewModel.sendMessageにも
  適用。1対1の私的な会話という性質を踏まえ、初回ガイドライン確認や違反カウント
  警告までは持ち込まず、保護の実処理のみを揃えた
- 前回からの変化: 4件の残課題は全て対応完了(1件は完全解決、1件は実機再確認待ち、
  1件は暫定的な安全策への切り替え、1件は実装完了)。次回フル監査では、
  cross-service連携の有効化余地、App Check再有効化の判断、18歳ゲートの実際の
  UX(該当ユーザーへの表示)を確認すること

## 2026-08-18 15:3x（初回監査）

- 総合判定: 🟡（P0は無いが、公開前に判断すべき法務・セキュリティ・事業面の項目が複数残る）
- 8分野スコア: 技術85/セキュリティ80/法務65/UX78/安全75/AI68/事業55/運営72
- P0件数: 0件
- P1件数: 3件（storage.rulesのchatMedia穴(修正済・未デプロイ)、Gemini API利用規約と年齢要件の矛盾、App CheckのEnforce状態未確認）
- 今回実際に修正した件数: 2件（storage.rules chatMediaにグループメンバーシップ検証を追加(未デプロイ)、Info.plistのNSPhotoLibraryUsageDescription拡充）
- リリースブロッカー: 無し（ただし要判断事項3点あり。上記P1参照）
- 前回からの変化: 初回のため無し。checkai/finalcheck/release-analyzerの直近履歴と突き合わせ、finalcheckの残課題5件中3件（Gemini年齢要件・APIキー埋め込み・匿名チャットのブロック導線）が今回も未解消と確認。release-analyzerのApp Check Enforce未確認も持ち越し。

## 2026-08-18 15:5x〜16:1x（初回レポート後、ユーザー不在時に優先順位に沿って継続対応）

ユーザーが3時間席を外す間、firestore.rulesのデプロイとAI URL解析機能には触れない前提で、レポートのP1/P2項目を優先順位順に自律対応した。

- 実際に修正・追加した項目:
  - events/{eventId}/{fileName}のStorageルールにもchatMediaと同型の「メンバー以外でも書き込み可」の穴があったため同じ手法で修正(未デプロイ)
  - 匿名チャット(AnonymousChatRoomView)に「ブロックする」を追加。既存のModerationService.blockUser/fetchBlockedUidsをそのまま使い、匿名性を保ったまま迷惑ユーザーを自分の画面から非表示にできるようにした
  - Analyticsイベント計測を拡充(sign_up/login/group_created/group_joined/user_followed/dm_sent)。以前は投稿・予定作成・シェア・AI利用の5種類のみで、成長ループの前半(登録〜フォロー〜DM)が全く追えていなかった
  - プライバシーポリシー(HTML版・アプリ内Swift版の両方)のGemini API利用範囲をURL抽出・当日ガイド生成分も含めて拡充。Hostingへデプロイしcurl疎通確認済み
  - 調査中に発見: アプリ内表示用TermsOfServiceView.swiftがHTML版(terms-of-service.html)より古く、有料プラン条項(第8条)が丸ごと欠落し、さらに「グループの管理者・オーナーがメンバーを強制退出できる」という2026/08/13に廃止済みの権限モデルを誤って記載したままだった。HTML版(全13条)に合わせて全文同期
- 調査したが「実は問題無し」と判明した項目（前回のスコア算出に使われた粗い指標が実態を過大に見積もっていたことが判明）:
  - アクセシビリティ: 469個のアイコンのうち139個のみラベル付与という比率から「多数の未対応ボタンがある」ように見えたが、実際にButton内アイコン単体(装飾でなく操作対象)を精査したところ、装飾アイコン(accessibilityHidden対応済み)かテキスト併記(Label/Text)のいずれかで、真に未対応の箇所はAI URL解析機能(対象外)を除き0件だった
  - ダークモード: Color.white/Color(hex:)の残存59ファイルも、実際に背景色として使われている箇所はLoginView.swift(意図的な「白背景エリア」design、動作確認済みで文字は視認可能)のみで、他はグラデーションボタンの文字色や影・装飾パーティクルなど、固定であるべき正当な用途だった
- 見つけたが未着手の項目（実装ではなく製品判断が必要と判断）:
  - DM(1対1)にNGWordFilter(自動伏せ字)が一切適用されていない。匿名/公開トークルームには既にあるが、DMは「プライベートな会話への自動検閲をすべきか」という製品判断が先に必要
  - 制限(restrictedUsers)されたユーザーへのエラーメッセージが「送信できませんでした」のみで理由を説明しない。restrictedUsersはクライアントから読み取り不可の設計のため、Firestoreのpermission-deniedエラーだけから「制限が原因」と正確に判定するのは技術的に脆く、実装前に方針確認が必要
- 前回からの変化: 初回レポート提示直後の同一セッション内での継続対応のため、総合判定・8分野スコアの再計算はまだ行っていない(次回フル監査時に反映)。P1の3件中1件(storage.rulesのchatMedia)は修正済みだが未デプロイのまま。

## 2026-08-18 16:3x〜18:4x（実機テストで発覚した重大問題への対応、ユーザーとの往復で根本原因を特定・解決）

実機での動作確認により、コードレビューだけでは見つけられなかった2つの重大な問題が発覚し、両方とも解決した。

- **App Check強制(Enforce)が原因で、Firestoreへのリクエストの最大63%が拒否されていた**：
  ユーザーから「Wi-Fi以外では使い物にならない」「前回の再試行ロジックの修正でも直っていない」との報告を受け、
  以前の対策(リスナーの再試行)が効かない理由を分析。Firebaseコンソールを確認してもらったところ、
  Cloud FirestoreのApp Checkが「適用済み」で、未検証・無効なリクエストが直近で最大63%に達していることが
  判明。ユーザーの判断でEnforceを解除し、起動・グループ読み込みの問題は解消した。App Check自体は
  匿名アカウント悪用防止のために導入したものだが、現状はApp Attestの検証失敗率が高すぎて実用に耐えない
  ため、原因(証明書プロバイダ設定等)を調べてから再度Enforceを有効化する必要がある(未着手)
- **予定・投稿・カレンダー等の保存処理が、通信不安定時に無期限にフリーズする系統的なバグ**：
  App Check解除後も「予定保存中に固まる」との報告があり調査。Firestoreの書き込み完了コールバックを
  待つ処理にタイムアウトが一切無く、応答が返らないと画面が永久に「保存中」のままになる作りだったことが
  発覚。NetworkMonitor.awaitWithTimeout(8秒)を新設し、予定の追加・編集、初回プロフィール登録、
  投稿全般(通常投稿・持ち物テンプレ・費用シミュレーター)、カレンダーの改名・メンバー変更・新規作成、
  会場口コミ投稿、思い出日記、の計8ファイルに適用した
- グループ退出時に「グループを退出しました」の表示が無かった(ユーザー要望)ため追加。エラーの握りつぶしも
  合わせて修正
- 前回の全画面デザインレビューの残課題のうち、マイページの投稿グリッド/グループの空状態表示、
  会場マップのスナップショット読込失敗時のフォールバック表示を追加対応
- 検討したが見送った項目：匿名チャットに「ブロック中のユーザーを管理」ショートカットを追加する案
  (OpenChatRoomViewには既にある)。ただし既存のBlockedUsersSheetは相手の実名・アイコンを表示する
  設計のため、そのまま流用すると「匿名でブロックしたはずが、後から設定画面で相手の本名が分かる」形に
  なり、匿名チャットの設計思想と矛盾する。UIの一貫性より製品判断を優先し、今回は見送った
- 前回からの変化: P0相当の重大な実害(通信環境を問わずアプリがほぼ機能しない状態)が2つ見つかり、
  両方解消。次回フル監査時は技術・セキュリティのスコアを大きく見直す必要がある(App Check再設計・
  Gemini APIキーのCloud Functions移行は依然未着手)
